plugins {
    id "idea"
    id "eclipse"
}

def taskNames = ["clean", "assemble", "test", "build"]

taskNames.each { name ->
    tasks.create(name) {
        def build = gradle.includedBuild("secured-app")
        it.dependsOn build.task(":$name")
    }
}
